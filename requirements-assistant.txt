# Optional only. The existing app and local retrieval need no extra packages.
# Keep inference dependency pins from requirements-all.txt when installing.
langchain[anthropic,openai]==1.4.2
langchain-anthropic==1.7.2
langchain-openai==1.6.2
fastapi==0.135.1
uvicorn==0.41.0
